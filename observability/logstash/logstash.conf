# ELT Pipeline

input {
  tcp {
    port => 5000
    codec => json
  }
}

filter {
  mutate {
    add_field => { "environment" => "local-dev" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "app-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
